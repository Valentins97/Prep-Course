Arrays: 
Podemos pensar en las matrices como contenedores de almacenamiento para colecciones de datos, 
construir una matriz es simple, viene conformada por una variable que se establece en [] , 
luego se agrega un contenedor (separado por coma) tantas cadenas, numeros o booleanos como queramos. 